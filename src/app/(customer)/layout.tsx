import { CustomerShellLayout } from "@/components/spec/shell-layout";

export default function CustomerLayout({ children }: { children: React.ReactNode }) {
  return <CustomerShellLayout>{children}</CustomerShellLayout>;
}
