import { CarrierShellLayout } from "@/components/spec/shell-layout";

export default function CarrierLayout({ children }: { children: React.ReactNode }) {
  return <CarrierShellLayout>{children}</CarrierShellLayout>;
}
